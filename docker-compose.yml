services:
  redis-vector:
    image: redis/redis-stack:latest
    container_name: redis-vector-db
    ports:
      - "6379:6379"
      - "8001:8001"
    volumes:
      - ./redis-data:/data
    environment:
      - REDIS_ARGS=--save 60 1 
    restart: always

volumes:
  redis-data: